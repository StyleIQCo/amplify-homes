type Shoes @model @auth(rules: [{allow: public}]) {
  id: ID!
  designer: String
  model: String
  price: Float
  url: AWSURL
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
}
 